## 1. 服务边界与配置基线

- [x] 1.1 盘点当前 workspace 脚本、Compose 服务、端口、环境变量和调用方，形成公开端点与 GPU 私有端点的迁移映射。
- [x] 1.2 定义 `video-streamer`、`gpu-inference-api` 和 `web` 的独立构建入口、镜像名称、配置样例和存活/就绪检查约定。
- [x] 1.3 将公开 API 地址、帧接收地址、推送令牌、允许的 Web 来源及内部运行时地址拆分为明确的环境变量，移除生产样例中对内部模型端口的依赖。
- [x] 1.4 为服务配置与端口边界添加自动校验，验证 Web、推流端和 GPU 私有运行时不会读取错误的地址类别。

## 2. 视频推流服务

- [x] 2.1 从现有视觉编排器提取或固定仅含 pusher 角色的 `video-streamer` 启动入口，保留 USB、RTSP 和 mock 视频源支持。
- [x] 2.2 实现受大小限制的 JPEG 推送、推送令牌认证、超时处理和最近一次传输状态，确保失败帧被丢弃而不落盘或排队。
- [x] 2.3 为推流服务提供独立存活/就绪端点及摄像头、配置缺失、接收端不可用的可区分状态。
- [x] 2.4 编写推流服务测试，覆盖三类输入源、认证失败、超时/恢复、帧大小限制和无本地视频归档。

## 3. GPU 推理 API 服务

- [x] 3.1 建立 `gpu-inference-api` 服务包入口，将 API、帧接收器、视觉编排、Florence、Grounding、SAM、Embedding 和 ASR 作为同一部署单元的私有运行时组织。
- [x] 3.2 将 API 数据库、证据目录和模型缓存挂载迁移到 GPU 服务包，保持 API 为观测事实和证据的唯一持久化写入方。
- [x] 3.3 将视觉与 ASR 调用改为 GPU 服务内部地址或受管理调用，并保留关联 ID、契约版本、超时、模型版本及成功/降级/失败结果。
- [x] 3.4 聚合 API、帧接收、关键视觉运行时和 ASR 的健康状态；提供受控的组件诊断信息而不公开内部服务端口。
- [x] 3.5 为模型不可用、ASR 未就绪、推理超时和 GPU 服务恢复实现可观测降级，确保文字查询和既有事实读取不会被阻断。
- [x] 3.6 编写 GPU 服务集成测试，覆盖受认证帧接收、观测写入边界、语音转写成功/失败、内部追踪字段和聚合健康状态。

## 4. Web 前端服务

- [x] 4.1 将 Web 配置收敛为唯一的公开 API 地址，删除对摄像头、推流和内部模型/ASR 地址的直接依赖。
- [x] 4.2 验证并调整文字查询、麦克风上传、转写失败展示、证据查看和浏览器语音播报，以保持既有用户交互与事实分层。
- [x] 4.3 配置 Web 对 GPU 公开 API 的跨域和部署时地址注入，并增加错误状态以区分 API 不可用与语音转写不可用。
- [x] 4.4 编写前端测试，验证不访问内部端口、公开 API 地址切换及文字/语音查询的可用性。

## 5. 部署、文档与验证

- [x] 5.1 重组 Docker Compose 与本地脚本，使开发和生产均可按三个服务包独立构建、启动、停止和升级，且 GPU 包不发布模型或 ASR 端口。
- [x] 5.2 更新 `.env` 示例、README、服务运行手册和远程推流说明，写明三服务部署顺序、公开网络面、私有端口和排障方式。
- [x] 5.3 移除 `gpu-inference-api` 的 CPU/mock Compose、环境变量、运行时分支和文档；验证缺少 GPU 或真实模型时服务明确未就绪。
- [ ] 5.4 在 GPU 节点执行预检与端到端验证，确认模型/ASR 就绪、显存限制、最新帧丢弃、认证推送、语音查询和五秒响应降级。
- [x] 5.5 执行现有 contracts、前端构建、Python 测试与新增部署测试；记录从旧 Compose 方式迁移和回滚到旧入口的演练结果。
