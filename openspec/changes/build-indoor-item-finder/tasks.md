## 1. 工程骨架与开发基线

- [x] 1.1 建立 pnpm 单仓库，创建 `apps/web`、`services/api`、`services/vision` 和 `packages/contracts` 目录及统一脚本
- [ ] 1.2 初始化 React + TypeScript + Vite 网页和两个 Python FastAPI 服务，配置格式化、静态检查和测试框架
- [x] 1.3 建立分层配置与 `.env.example`，覆盖摄像头、数据库、证据目录、模型、LLM、ASR、阈值和存储预算
- [ ] 1.4 添加统一开发启动命令、健康检查和最小 smoke test，验证三个服务可以同时启动并互相访问

## 2. 类型化合同与持久化

- [x] 2.1 定义 Room、Object、Alias、Location、Observation、ObjectState、Evidence、Prediction 和 QueryResult 的 Pydantic/OpenAPI 合同
- [x] 2.2 实现 SQLite + WAL 数据层、版本化迁移以及规范名称归一化唯一索引
- [x] 2.3 实现 ULID 身份生成，保证物品和位置重命名不改变 ID 或历史关联
- [ ] 2.4 生成 TypeScript API 客户端并添加前后端状态枚举一致性检查
- [ ] 2.5 添加数据层单元测试，覆盖唯一名称冲突、重命名、事务回滚和历史关联

## 3. 摄像头与有界证据存储

- [ ] 3.1 实现 USB 摄像头、RTSP 和录制视频夹具三种 frame source adapter，采用最新帧优先的有界队列
- [ ] 3.2 实现摄像头状态、断线重连、画面时间戳和网页预览接口
- [ ] 3.3 实现证据 JPEG、缩略图和边界/掩码元数据写入，禁止连续录像写盘
- [ ] 3.4 实现每物品数量上限、全局字节预算、最新证据保护和孤儿文件清理
- [ ] 3.5 添加录像夹具测试，验证断线状态、证据变化触发、第二十一张覆盖和全局预算清理

## 4. 开源视觉模型服务

- [ ] 4.1 建立统一 detector、captioner、segment-tracker、embedding 和 OCR adapter 接口及模型就绪状态
- [ ] 4.2 集成 Florence-2-base-ft 的区域检测/描述，输出规范化候选类别与可见属性
- [ ] 4.3 集成 Grounding DINO 的开放词汇物品和卧室静态位置复核
- [ ] 4.4 集成 SAM 2.1 Hiera Tiny 的掩码生成与多目标视频跟踪
- [ ] 4.5 集成 DINOv2 ViT-S/14、颜色和 OCR 特征，建立有界代表特征库
- [ ] 4.6 实现 GPU 和 CPU 配置 profile、模型预热、逐级降频和清晰的模型不可用错误
- [ ] 4.7 使用固定图像/短视频夹具验证每个 adapter 的输出 schema、模型降级和重启恢复

## 5. 自动物品与位置目录

- [x] 5.1 实现多帧候选晋级状态机，短暂或低置信候选不得直接进入正式目录
- [ ] 5.2 实现结合 track、时空、向量、颜色/OCR 和类别的一对一身份匹配及 `identity_uncertain` 分支
- [ ] 5.3 实现自动中文物品名称生成、唯一名称消歧、系统生成标记、用户重命名和别名管理
- [x] 5.4 实现至少十个具体物品的目录容量与同类别实例分别建档
- [ ] 5.5 实现床、书桌、床头柜、衣柜、门、地面等静态区域的多帧发现、唯一命名和层级几何
- [ ] 5.6 实现位置重命名和多边形修正，保留 `location_id` 与历史名称快照
- [ ] 5.7 添加双水杯、相似物品不确定、名称冲突和双床头柜位置消歧的集成测试

## 6. 位置记忆与推测

- [ ] 6.1 实现观测稳定规则、新鲜度窗口、不可见宽限及 `object_state` 快照更新
- [ ] 6.2 实现物品掩码到唯一位置区域的归属，以及左/中/右、上/下等确定性关系描述
- [ ] 6.3 实现首次出现、位置变化、身份变化和消失前最后观测的事件与证据固化
- [ ] 6.4 实现最后出现位置/时间/证据查询，确保其不会被标记为当前位置
- [ ] 6.5 实现基于最后位置、消失邻域和历史迁移的最多三个可审计预测及置信等级
- [ ] 6.6 添加当前可见、被遮挡、离开画面、身份不确定、无区域和无推测依据的状态机测试

## 7. LLM 问答编排

- [ ] 7.1 实现规范名称、别名和模糊候选召回，多个同类候选时返回澄清选项
- [ ] 7.2 实现 OpenAI-compatible LLM provider、`FindObjectIntent` 工具 schema 和本地 Qwen 配置样例
- [ ] 7.3 实现结构化事实包、事实与预测分区的中文回答提示词和输出校验器
- [ ] 7.4 实现 2.5 秒 LLM 超时、格式错误、无服务时的确定性解析与中文模板降级
- [ ] 7.5 实现 `/api/query`，返回文字回答、状态、事实、预测、证据引用和分段耗时
- [ ] 7.6 添加提示注入、补造位置、候选越权、LLM 超时、无匹配和歧义查询测试

## 8. 网页语音输入与播报

- [ ] 8.1 实现 `MediaRecorder` 麦克风录制、权限状态、音频上传和可编辑转写文本
- [ ] 8.2 集成 faster-whisper ASR adapter，并提供浏览器 SpeechRecognition 可选快速路径
- [ ] 8.3 实现 `/api/speech/transcribe` 的格式校验、时长限制、中文转写和错误降级
- [ ] 8.4 实现浏览器 `speechSynthesis` 自动播报、重播、停止和静音控制，确保不阻塞文字显示
- [ ] 8.5 使用录制语音夹具和前端 mock 验证成功转写、权限拒绝、ASR 失败与停止播报

## 9. 响应式网页产品原型

- [x] 9.1 建立产品导航、运行状态栏、摄像头预览和桌面/移动端响应式布局
- [ ] 9.2 实现问答主页，包括文本输入、麦克风、处理状态、会话记录、澄清选择和错误状态
- [x] 9.3 实现区分“当前检测”“当前未检测”“最后出现”“推测”的回答视觉层级和最多三个候选位置
- [x] 9.4 实现证据查看器，展示图片、目标标记、观测时间、唯一物品和位置名称
- [ ] 9.5 实现物品目录页面，支持系统名称/置信信息、重命名、别名和身份不确定状态
- [ ] 9.6 实现位置目录和摄像头区域编辑器，支持唯一名称校验和多边形修正
- [ ] 9.7 添加 Playwright 端到端测试，覆盖文字查找、语音 mock、同类澄清、证据展开和移动端视口

## 10. 端到端验收与性能

- [ ] 10.1 制作包含至少十个具体物品、两个同类物品、移动和遮挡过程的可重复卧室视频夹具
- [ ] 10.2 建立从视频观测到网页回答的端到端验收脚本，核对 object/location ID、状态和证据
- [ ] 10.3 建立二十次混合查询性能基准，输出 `resolve_ms`、`lookup_ms`、`llm_ms` 和 `total_ms`
- [ ] 10.4 调整后台帧率、阈值、模型 profile 和查询超时，使预热环境至少十九次查询在五秒内返回
- [ ] 10.5 运行严格 OpenSpec 验证、前端测试、后端测试、模型夹具测试和端到端测试并保存结果摘要

## 11. 部署与运行脚本

- [x] 11.1 编写 web、api、vision 的生产 Dockerfile 和 Docker Compose 基础配置
- [x] 11.2 添加 NVIDIA GPU override、CPU 兼容 profile、模型缓存卷和证据/数据库持久卷
- [ ] 11.3 编写模型下载/校验脚本、数据库迁移脚本及 Windows PowerShell 一键启动/停止脚本
- [ ] 11.4 添加首次启动检查，验证摄像头、麦克风提示、模型权重、LLM/ASR、端口和目录写入权限
- [ ] 11.5 在干净环境执行部署 smoke test，验证停止和重启不会删除目录、历史或证据

## 12. 产品说明与交付

- [ ] 12.1 编写产品说明书，说明目标用户、核心承诺、自动发现流程、问答状态、推测边界和首版非目标
- [x] 12.2 编写安装部署、硬件 profile、摄像头配置、模型配置、故障排查和数据目录说明
- [ ] 12.3 编写现场演示流程，覆盖自动生成十个物品、同类区分、当前位置、遮挡后的最后位置与推测、语音播报
- [ ] 12.4 核对交付清单仅包含产品原型、前后端代码、模型服务、部署脚本和产品说明书
- [ ] 12.5 完成最终验收记录，并在实现验证通过后执行 OpenSpec sync/archive 流程
