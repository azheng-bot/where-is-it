## 1. 内部契约与运行配置

- [x] 1.1 在共享 contracts 包中定义版本化的推理请求、响应、错误状态、关联 ID 和媒体引用模式，并提供 API 与 Python 服务使用的校验类型。
- [x] 1.2 为所有服务添加模型服务地址、超时、模型版本、运行模式和并发/设备配置；默认配置保持本地 mock 演示可启动。
- [x] 1.3 为内部 HTTP 请求实现统一的关联 ID、截止时间传播、结构化错误映射和服务客户端基础层。
- [x] 1.4 为内部契约和配置默认值编写单元测试，覆盖版本不兼容、缺失关联 ID、超时与未配置服务。

## 2. 视觉编排与 mock 迁移

- [x] 2.1 新建无 GPU 的 `vision-orchestrator` 服务，提供存活、就绪和帧处理入口，并复用现有 API 观测上报契约。
- [x] 2.2 将现有卧室 mock 图、循环帧源和固定观测迁移为 orchestrator 的可选 mock 适配器，删除对前端坐标的依赖。
- [x] 2.3 实现发现、目标定位、分割和嵌入阶段的预算分配、结果归一化与失败/降级处理，确保未验证结果不会上报为位置事实。
- [x] 2.4 验证无真实模型配置时，mock 摄像头可经 orchestrator 上报并在网页中显示与证据一致的标注框。

## 3. 独立视觉模型服务

- [x] 3.1 为 Florence 服务建立独立进程、`/health/live`、`/health/ready` 和 `/v1/infer` 契约实现，并通过 mock 后端验证启动与模型未就绪状态。
- [x] 3.2 为 Grounding 服务建立同等服务接口，实现候选目标定位结果的契约校验和超时错误返回。
- [x] 3.3 为 SAM 服务建立同等服务接口，实现框/掩码结果的契约校验和就绪检查。
- [x] 3.4 为 Embedding 服务建立同等服务接口，实现特征结果的契约校验和就绪检查。
- [x] 3.5 将四个模型服务接入 orchestrator，并为服务不可用、契约不兼容、超时和部分链路降级编写集成测试。

## 4. ASR 微服务与查询接入

- [x] 4.1 新建 ASR 服务，提供独立健康检查、版本化转写接口和可配置 CPU/GPU 后端。
- [x] 4.2 在 API 中实现受限的语音媒体验证、ASR 客户端、转写结果/错误映射，并把成功文本复用到现有查询流程。
- [x] 4.3 在 Web 中展示已使用的转写文本及可区分的语音处理失败，不影响键盘查询路径。
- [x] 4.4 编写语音成功、ASR 超时、ASR 未配置与键盘查询不受影响的端到端测试。

## 5. 本地运行、部署与可观测性

- [x] 5.1 更新开发启动编排，使 API、Web、orchestrator、mock/真实模型服务和 ASR 可以按环境配置独立启动。
- [x] 5.2 提供容器化部署定义和 GPU 服务的独立资源/设备配置，默认不假定所有模型可同时驻留同一张 GPU。
- [x] 5.3 为服务就绪、模型版本、请求关联 ID、时延、降级和错误码增加健康探测与结构化日志。
- [x] 5.4 验证 API 在任一模型服务或 ASR 服务不可用时仍可返回历史事实/明确错误，且不会产生伪造观测。
- [x] 5.5 执行完整构建、契约测试、mock 演示回归和服务边界集成测试，记录首个真实模型接入前的验收结果。
